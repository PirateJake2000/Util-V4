-- This file's name begins with "__" to be at the top when alphabetically ordered
-- Iterating through files in JS is done in alphabetical order by default I assume, and we want the framework definition to be at the top to avoid errors like "attempt to index a nil value"

-- Util table
Util = {
    Players = {},
    Vehicles = {},
    Events = {},
    Commands = {},
    Timers = {}
}